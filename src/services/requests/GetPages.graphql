query getPages($culture: String!) {
  contentByAbsoluteRoute(route: "/", culture: $culture) {
    children {
      properties {
        value{
          ... on BasicPropertyValue {
            alias
            value
          }
        }
      }
    }
  }
}