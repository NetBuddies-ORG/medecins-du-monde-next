query getPage($locale: I18NLocaleCode,$filters: PageFiltersInput) {
    pages(locale: $locale,filters: $filters){
        data{
            attributes{
                Titre
                ContentType
                ...BlocksComposition
            }
        }
    }
}