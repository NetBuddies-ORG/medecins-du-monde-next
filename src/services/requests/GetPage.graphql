query getPage($url: String!) {
  contentByAbsoluteRoute(route: $url) {
      properties {
        value{
          ... on BasicPropertyValue {
            alias
            value
          }
        }
      }
  }
}