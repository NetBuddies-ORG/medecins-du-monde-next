query getCategories($locale: I18NLocaleCode) {
    categories(locale: $locale, pagination: { limit: 1000 }){
        data{
            id
            attributes{
                Nom
                Icone
                sous_categories{
                    data{
                        id
                        attributes{
                            Nom
                        }
                    }
                }
            }
        }
    }
}