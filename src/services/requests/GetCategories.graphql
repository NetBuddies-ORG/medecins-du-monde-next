query getCategories($locale: I18NLocaleCode) {
    categories(locale: $locale){
        data{
            id
            attributes{
                Nom
                Icone
            }
        }
    }
}