query getPublics($locale: I18NLocaleCode) {
    publicSpecifiques(locale: $locale, pagination: { limit: 1000 }){
        data{
            id
            attributes{
                Nom
            }
        }
    }
}