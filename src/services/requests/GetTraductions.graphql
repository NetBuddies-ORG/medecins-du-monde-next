query getTranslations($locale: I18NLocaleCode){
    traductions(locale: $locale, pagination: { limit: 1000 }){
        data{
            attributes{
                Traduction
                Key
            }
        }
    }
}